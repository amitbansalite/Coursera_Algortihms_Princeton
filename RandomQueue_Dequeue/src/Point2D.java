
// Given 3 points (a,b,c) : determine whether a->b->c is a counterclockwise trun or not

// compare slope of line a-c to line a-b 
public class Point2D {

	/*private final double x;
	private final double y;
	
	public Point(double x, double y){
		this.x = x;
		this.y = y;
	}
	
	public static int ccw(Point a, Point b, Point c){
		int area2 = (b.x - a.x)*(c.y - a.y) - (b.y - a.y)*(c.x - a.x);
		
		if (area2 < 0)			// clockwise 
			return -1;
		else if (area2 > 0) 	// counter clockwise
			return 1;
		else return 0;			// collinear
	}	*/
}
